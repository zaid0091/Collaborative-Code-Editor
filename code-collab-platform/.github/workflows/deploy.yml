name: Deploy

# Full deploy automation is Phase 3. This file is a placeholder.

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    name: Deploy placeholder
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy placeholder
        run: |
          echo "Deploy workflow triggered after successful CI on main."
          echo "TODO: docker build + push to ECR"
          echo "TODO: ECS deploy rolling update"
          echo "TODO: run database migrations"
